# express_tutorial

실행순서
```bash
$ express express_tutorial -e -v ejs --git

# change directory:
$ cd express_tutorial

# install dependencies:
$ npm install

# run the app:
$ DEBUG=express-tutorial:* npm start
```
